# 《爱情银行》落地包 V0.2

> 这不是一份“功能堆叠”的产品说明，而是一份把初心落到可验证产品上的工作底稿。
>
> 当前核心判断：爱情银行首先要解决的不是“如何每天打卡”，而是“如何把散落在不同平台、不同时间里的共同生活，重新聚成一条属于两个人的轨迹”。

## 1. 一句话定义

爱情银行是一个只属于两个人的私密记忆账户：它把照片、视频、音频、聊天、文字和生活碎片汇聚起来，在事后轻轻整理成共同生活的轨迹；AI 只做提取、关联和观察，不给关系打分，也不替两个人下结论。

## 2. V0.2 要先验证什么

V0.2 不追求一次做成完整 App，只验证三个问题：

1. 用户愿不愿意把已有资料导入一个“只属于我们”的空间。
2. 系统能不能把一堆碎片整理成“某一天 / 某一段时间发生过什么”。
3. 当用户低落或遗忘时，回看这些共同积累，是否比单独浏览相册更有支撑感。

### 暂不做的事

- 不做爱情分数、关系健康度、匹配度或“谁付出更多”。
- 不做公开动态、社交推荐、排行榜和连续打卡奖励。
- 不要求用户每天填写完整日记。
- 不一开始就接入所有平台的实时 API。
- 不把“数字遗产”做成默认开启的自动转交；它必须是单独、明确、可撤销的授权流程。

## 3. 最小可用产品（MVP）

### 3.1 两人账户

- 一个人创建“我们的账户”，生成邀请链接或邀请码。
- 另一人接受邀请后，双方成为同一私密空间的共同成员。
- 每个成员可以看到空间内的内容，但删除、导出、遗产授权等高风险操作需要更清晰的确认提示。
- 默认不公开；搜索引擎、陌生用户和平台内推荐都不能看到内容。

### 3.2 四个轻量入口

用户完成一次记录的时间目标：5 秒到 30 秒。

| 入口 | 最小动作 | 系统后续处理 |
| --- | --- | --- |
| 一张照片 | 选择一张照片，可不写字 | 提取时间、地点（若用户允许）、画面主题，等待用户确认 |
| 一段语音 | 录 10 秒到 2 分钟 | 转写为文字，保留原音频，生成可搜索摘要 |
| 一句话 | 写一句当时想留下的话 | 关联相邻照片、聊天片段和日期 |
| 一个符号 | 选择 ❤️、🍜、🐾、☀️ 等 | 作为低负担的生活标记，不强行解释 |

### 3.3 第一个“有感觉”的结果

先做“共同的一天”，而不是先做复杂图表。

示例：

> 2026 年 8 月 18 日
>
> 早上 08:12 有一条问候；午餐出现了面；傍晚一起散步；相册里有 3 张照片；聊天中出现了 6 次昵称；晚上留下了一句“今天也很开心”。
>
> AI 观察：这一天出现了“问候—一起吃饭—散步—晚间收尾”的重复节奏。
>
> 这不是关系判断，只是把已经发生过的内容重新摆在一起。

### 3.4 先手动、后自动的导入路径

第一版建议只支持三种可控导入方式：

1. 上传照片、视频、音频和文字文件。
2. 粘贴一段聊天记录文本，或上传导出的 `.txt` / `.html` 文件。
3. 上传一个已有文件夹的压缩包，由系统先做本地化整理，再让用户确认导入范围。

导入结果必须经过“待确认”状态，不能让 AI 直接把未经确认的推断写成事实。

## 4. 核心使用流程

### 流程 A：创建共同账户

创建空间 → 邀请伴侣 → 选择隐私设置 → 看到空白的“我们的账户” → 添加第一张记忆卡片。

### 流程 B：导入三年聊天记录

上传聊天文件 → 系统识别日期、发送者、文本和附件引用 → 展示抽样预览 → 用户确认“哪些人、哪些日期、哪些内容可以进入账户” → 生成时间线 → 用户修改或删除错误结果。

### 流程 C：回看共同生活

打开首页 → 看到最近一段共同轨迹 → 进入某一天 → 查看原始素材与整理后的叙述 → 切换“只看发生过的事 / 看 AI 观察” → 导出这一段为可阅读文件。

### 流程 D：低谷时刻

用户主动点击“看看我们走过的路” → 系统展示一段时间内的共同日常、关键节点和原始素材入口 → 不播放煽情文案，不给出“你们应该怎样”的建议。

## 5. 信息架构

建议第一版只有五个一级入口：

| 页面 | 作用 |
| --- | --- |
| 我们 | 看到共同账户的总体轮廓和最近轨迹 |
| 时间线 | 按天、周、月回看生活片段 |
| 添加 | 快速新增一张记忆卡片 |
| 导入 | 管理聊天、照片、视频、音频等外部资料 |
| 账户与主权 | 成员、隐私、导出、删除、迁移、遗产授权 |

“余额”可以作为叙事隐喻，但界面不应出现把爱情量化成一个分数的视觉语言。可以展示“共同走过 1,287 天”“已留下 4,832 个片段”，但这些数字只是规模，不是价值评价。

## 6. AI 的边界

### AI 可以做

- 从聊天记录中提取时间、发送者、昵称、地点和重复出现的主题。
- 从照片、音频和文字中提取可检索信息。
- 把同一天或相邻时间的不同来源素材聚合到一起。
- 发现可验证的重复模式，例如“周末经常一起散步”“某个昵称在晚间更常出现”。
- 生成“待确认”的摘要、标签和时间线草稿。

### AI 不可以做

- 判断谁更爱谁、谁付出更多、关系是否健康。
- 把沉默、争吵、词频或表情直接解释为情感结论。
- 未经确认把推断写成事实。
- 使用用户内容训练其他用户可见的模型或推荐系统。
- 用“为了你们好”为理由替用户自动联系、提醒或公开内容。

### 每条观察的显示格式

每条 AI 观察都应能展开查看：

1. 观察内容：系统看到了什么。
2. 证据来源：来自哪些日期、哪些素材。
3. 置信度：高 / 中 / 低，或直接使用“待确认”。
4. 用户操作：确认、修改、隐藏、删除、不再生成类似观察。

## 7. 数据主权与迁移设计

### 原则

- 原始文件永远保留，不用 AI 摘要替代原件。
- 每一条整理结果都能追溯到原始来源。
- 导出不应只导出一个平台专属格式。
- 用户可以删除、下载、迁移，不因停止订阅而失去读取权。

### 建议的导出包

```text
love-bank-export-YYYY-MM-DD/
├── manifest.json          # 导出时间、版本、文件清单、校验值
├── memories.jsonl         # 统一记忆卡片格式
├── observations.jsonl     # AI 观察及其证据引用
├── timeline.csv           # 方便用表格打开的时间线
├── media/
│   ├── photos/
│   ├── videos/
│   └── audio/
├── originals/             # 原始聊天文件与原始附件（若用户选择）
└── readable/
    ├── timeline.html
    └── timeline.pdf
```

建议把 `manifest.json` 和 `memories.jsonl` 作为长期稳定的公开格式。未来即使产品停止服务，用户仍能用普通文本工具读取自己的共同资产。

## 8. 推荐技术方案（给开发者看的版本）

第一阶段推荐做 Web/PWA，而不是直接做两个原生 App：更容易导入文件、分享测试链接、快速迭代，也方便后续再封装成移动端。技术选型保持可替换，避免把用户锁进某一家平台。

### 8.1 分层架构

<div style="font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:#f8fafc;border:1px solid #e2e8f0;border-radius:16px;padding:18px;color:#0f172a;max-width:1100px"><div style="display:grid;grid-template-columns:1fr 1.5fr 1fr;gap:12px;align-items:stretch"><div style="background:#eff6ff;border:1px solid #bfdbfe;border-radius:12px;padding:14px"><div style="font-weight:700;color:#1d4ed8;margin-bottom:8px">用户层</div><div style="display:grid;gap:8px"><div style="background:white;border-radius:8px;padding:9px">Web / PWA<br><small>两人共同账户、时间线、导入、导出</small></div><div style="background:white;border-radius:8px;padding:9px">移动端入口<br><small>快速拍照、录音、符号记录</small></div></div></div><div style="display:grid;gap:12px"><div style="background:#f0fdf4;border:1px solid #bbf7d0;border-radius:12px;padding:14px"><div style="font-weight:700;color:#15803d;margin-bottom:8px">应用层</div><div style="display:grid;grid-template-columns:repeat(2,1fr);gap:8px"><div style="background:white;border-radius:8px;padding:9px">账户与成员<br><small>邀请、权限、共同空间</small></div><div style="background:white;border-radius:8px;padding:9px">记忆与时间线<br><small>卡片、聚合、检索</small></div><div style="background:white;border-radius:8px;padding:9px">导入导出<br><small>任务状态、格式转换</small></div><div style="background:white;border-radius:8px;padding:9px">主权控制<br><small>删除、迁移、遗产授权</small></div></div></div><div style="background:#fff7ed;border:1px solid #fed7aa;border-radius:12px;padding:14px"><div style="font-weight:700;color:#c2410c;margin-bottom:8px">AI / 处理层</div><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px"><div style="background:white;border-radius:8px;padding:9px">解析<br><small>文字、图片、音频</small></div><div style="background:white;border-radius:8px;padding:9px">关联<br><small>时间、来源、附件</small></div><div style="background:white;border-radius:8px;padding:9px">观察<br><small>可验证、可撤回</small></div></div></div></div><div style="background:#f5f3ff;border:1px solid #ddd6fe;border-radius:12px;padding:14px"><div style="font-weight:700;color:#6d28d9;margin-bottom:8px">数据与基础设施</div><div style="display:grid;gap:8px"><div style="background:white;border-radius:8px;padding:9px">关系数据库<br><small>成员、记忆、来源、权限</small></div><div style="background:white;border-radius:8px;padding:9px">对象存储<br><small>照片、视频、音频、原始文件</small></div><div style="background:white;border-radius:8px;padding:9px">后台任务队列<br><small>导入、转写、索引、导出</small></div><div style="background:white;border-radius:8px;padding:9px">审计与加密<br><small>访问记录、密钥、备份</small></div></div></div></div><div style="margin-top:12px;padding:10px 12px;border-radius:10px;background:#fff;border:1px dashed #94a3b8;color:#475569;font-size:13px"><strong>外部来源：</strong>聊天导出文件、相册/云盘文件、用户手动上传；第一版以“用户主动上传”为主，避免一开始依赖大量第三方 API。</div></div>

### 8.2 核心数据对象

| 对象 | 作用 | 必须保留的主权信息 |
| --- | --- | --- |
| `couple_space` | 两人的共同空间 | 创建者、成员、创建时间、状态 |
| `memory_item` | 一张统一的记忆卡片 | 原始来源、时间、作者、内容类型、可见范围 |
| `source_asset` | 原始照片、音频、聊天文件等 | 原文件路径、哈希、格式、导入批次 |
| `timeline_event` | 某一天或一段时间的聚合 | 参与的记忆卡片、时间范围、人工修订记录 |
| `ai_observation` | AI 生成的观察 | 证据引用、模型版本、置信度、确认状态 |
| `consent_record` | 成员授权记录 | 谁在何时同意了什么、是否撤回 |
| `export_job` | 导出任务 | 范围、格式、生成时间、下载有效期 |
| `legacy_policy` | 数字遗产策略 | 是否开启、触发条件、接收人、撤销记录 |

### 8.3 最小 API 能力

- `POST /spaces`：创建共同空间。
- `POST /spaces/{id}/invites`：生成邀请。
- `POST /memories`：新增记忆卡片。
- `POST /imports`：创建导入任务。
- `GET /imports/{id}`：查看导入进度与待确认结果。
- `GET /timeline`：按时间范围读取共同轨迹。
- `POST /observations/{id}/confirm`：确认、修改或隐藏 AI 观察。
- `POST /exports`：创建导出包。
- `GET /exports/{id}/download`：下载导出包。

## 9. 安全与隐私底线

MVP 就要有的底线：

- 全程 HTTPS，存储加密，媒体文件使用短时效下载链接。
- 每个共同空间严格隔离，服务端每一次读取都校验空间成员身份。
- 聊天、音频和图片默认不用于训练面向其他人的模型。
- 导入、查看、下载、删除、导出都留下可查看的访问记录。
- 删除前明确说明范围；“删除 AI 观察”不能误删原始素材。
- 高风险操作提供二次确认和冷静期；真正删除前允许撤销。
- 数字遗产功能默认关闭，单独设置、单独说明、单独确认。

这里的“安全”是产品设计要求，不等于已经完成法律合规认证。正式上线前还需要根据部署地区补充隐私政策、数据处理协议和未成年人使用边界。

## 10. 8 周落地节奏

| 周期 | 产出 | 验证重点 |
| --- | --- | --- |
| 第 1 周 | 交互原型、数据字典、隐私原则 | 用户是否理解“共同账户”而非“情侣打卡” |
| 第 2 周 | 两人账户、邀请、基础记忆卡片 | 双方是否愿意共同使用 |
| 第 3 周 | 图片/文字/语音上传与原始文件保存 | 记录是否足够轻 |
| 第 4 周 | 聊天文本导入与预览 | 导入是否让人感到可控、可撤回 |
| 第 5 周 | 时间线与“共同的一天” | 散落素材是否被整理成生活轨迹 |
| 第 6 周 | AI 提取与待确认观察 | AI 是否温柔、可解释、不越界 |
| 第 7 周 | 导出包、可读 HTML、删除流程 | 用户是否真正拥有数据 |
| 第 8 周 | 5 对情侣试用与复盘 | 是否产生持续回看的理由 |

## 11. 不会编程时也能马上做的验证

在真正开发前，可以先做一个 7 天手动试验：

1. 建一个本地文件夹，按日期保存照片、语音、聊天片段和一句话。
2. 每天只记录一张“记忆卡片”，不要追求完整。
3. 第 7 天手动把同一天的素材拼成一张“共同的一天”。
4. 两个人分别回答：哪些内容让你想起了当时的感觉？哪些内容完全不重要？哪些内容你希望系统自动找出来？
5. 把答案写回产品规则，而不是急着增加功能。

这一步的价值，是先验证“回看什么会带来支撑感”，避免把大量时间花在用户并不想看的图表上。

## 12. 当前最重要的产品决定

建议先把产品定成：

> “一个可迁移、可回看、只属于两个人的共同生活档案。”

而不是：

> “一个帮助情侣经营感情的智能 App。”

前者强调数据主权、共同资产与记忆的真实性；后者容易滑向打卡、评价、建议和关系干预，最终违背最初的初心。

## 13. 已交付的界面原型

原型文件：[爱情银行-核心体验原型.html](./爱情银行-核心体验原型.html)

它包含三种结构不同的首页方案，可用底部切换条或 URL 参数 `?variant=a`、`?variant=b`、`?variant=c` 切换。原型使用的是虚构数据，不连接网络，也不会保存真实内容；它的目的只是帮助你判断“我们”这个首页应该更像账户、时间线，还是资料库。

下一轮最值得做的不是继续加功能，而是选出一个首页方向，然后拿 3—5 对情侣做一次“导入一段聊天记录 → 看共同的一天”的小测试。
