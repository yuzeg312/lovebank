## Agent skills

### Issue tracker

本仓库的 issue 与规格以 GitHub issue 形式管理。参见 `docs/agents/issue-tracker.md`。

### Triage labels

issue 使用五个标准的 triage 角色标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）：仓库根目录下一个 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。
